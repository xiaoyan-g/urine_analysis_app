# urine_analysis_app
A application that uses computer vision to detect color pads on a urine test strip. By combining a YOLO8v (object detection) model with a database (csv), the user should be able to input an image of their test strip on white background and have the application output the detected values.
